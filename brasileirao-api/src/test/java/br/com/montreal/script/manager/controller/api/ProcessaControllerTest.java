package br.com.montreal.script.manager.controller.api;

import org.junit.Test;



public class ProcessaControllerTest {
	
	@Test
	public void call() {
		/*ExecucaoBean processamentoBean = Mockito.mock(ExecucaoBean.class);
		BDDMockito.when(processamentoBean.geraToken()).thenReturn("1234");
		
		ProcessaController processaController = new ProcessaController(processamentoBean);
		
		InputProcessamento regra = new InputProcessamento();
		String out = processaController.post(regra);
		
		assertThat(out).isEqualTo("1234");*/
	}

}
